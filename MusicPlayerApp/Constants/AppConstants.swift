import Foundation
import SwiftUI

/// Common Used app constants
enum AppConstants {
    enum Colors {
        static let background = Color(#colorLiteral(red: 0.8980392157, green: 0.9333333333, blue: 1, alpha: 1))
        static let primary = "#007AFF"
        static let secondary = "#6C757D"
    }
    
    enum Strings {
        static let appTitle = "🎶 Music Player"
        static let onlineTrack = "ONLINE TRACKS"
        static let offlineTrack = "OFFLINE TRACKS"
        static let error = "ERROR"
        static let ok = "OK"
        static let settings = "Settings"
    }
    
}
